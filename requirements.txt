# Memory Vault Dependencies

# Core cryptography
pynacl>=1.5.0

# Optional: TPM support (requires TPM hardware)
tpm2-pytss>=2.1.0; platform_system=="Linux"

# Optional: Physical token support
fido2>=1.1.0      # For FIDO2/U2F tokens (YubiKey, Nitrokey, etc.)
pyotp>=2.8.0      # For TOTP/HOTP fallback

# Standard library (included with Python 3.7+)
# - sqlite3
# - json
# - hashlib
# - uuid
# - datetime
# - base64
